"use client";

import React, { Fragment } from "react";

import type { Player } from "@/lib/types";

function HotnColdGame({ player }: { player: Player }) {
  return (
    <>
      <></>
    </>
  );
}

export default React.memo(HotnColdGame);
